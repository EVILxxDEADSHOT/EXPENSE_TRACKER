The The following is a python code for the graphical user interface for the expense tracker, which have two menus One for income and other for expense It has four enter fields for entering the data. And the data stored in the Mysql workbench
There are three different codes, 1 which includes piegraph 1 which includes non pygraph and other is the proper representation of everything
